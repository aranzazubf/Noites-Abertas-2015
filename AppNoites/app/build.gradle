apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'NOITES'
            keyPassword 'noitesabertas'
            storeFile file('C:/Users/Arantxa/Dropbox/DesarrolloApp/Proyectos_android/Documentacion_android/ksaranzazubf.jks')
            storePassword '20011988!aBF'
        }
    }
    compileSdkVersion 'Google Inc.:Google APIs:21'
    buildToolsVersion '20.0.0'
    defaultConfig {
        applicationId "com.aranzazu.appnoites15"
        minSdkVersion 10
        targetSdkVersion 21
    }
    buildTypes {
        release {
            minifyEnabled false // new version
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
        debug {
            signingConfig signingConfigs.config
        }
    }
    productFlavors {
    }
}

dependencies {
    compile 'com.android.support:support-v4:21.+'
    compile 'com.google.android.gms:play-services:+'
}
